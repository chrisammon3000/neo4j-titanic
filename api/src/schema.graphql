type User {
  userId: ID!
  userHandle: String!
  userFirstName: String
  userLastName: String
  userBio: String
  userProfile_BG: String
  userProfile_FG: String
  userEmail: String
  userPassword: String
  userGender: String
  userSiteName: String
  userFollowers: [User] @cypher(statement: "MATCH (this)<-[:FOLLOWS]-(follower:User) RETURN follower.userHandle")
}

type Project {
  projectId: ID!
  projectName: String
  projectDescription: String
  projectCreatedBy: [User!]
  projectCreatedDate: Date
}

type Image {
  imageId: ID!
  imageOwner: [User!]
  imageCaption: String
  imageDescription: String
  imageCreatedDate: Date
  imageURL: String
}

type Tag {
  tagId: ID!
  tagName: String
  tagCreatedBy: String
  tagCreatedDate: Date
}
